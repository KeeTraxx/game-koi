[workspace]
resolver = "3"
members = ["crates/game-koi-core", "crates/game-koi-desktop", "crates/game-koi-web"]

# Shared by every crate in the workspace, so a version bump happens in one place.
[workspace.package]
version = "0.1.0"
edition = "2024"
